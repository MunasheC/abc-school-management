# Application Name
spring.application.name=School Management System

# Server Port Configuration
server.port=8080

# MySQL Database Configuration
spring.datasource.url=jdbc:sqlserver://10.106.60.33:1433;databaseName=school_management;encrypt=true;trustServerCertificate=true;
spring.datasource.username=sa
spring.datasource.password=P@ssw0rd123
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA/Hibernate Configuration
# update: Updates the schema if needed (good for development)
spring.jpa.hibernate.ddl-auto=update
# Show SQL queries in console (helpful for learning!)
spring.jpa.show-sql=true
# Format the SQL queries for better readability
spring.jpa.properties.hibernate.format_sql=true
# MySQL dialect
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# File Upload Configuration (for Excel files)
# Maximum file size (10MB)
spring.servlet.multipart.max-file-size=10MB
# Maximum request size (10MB)
spring.servlet.multipart.max-request-size=10MB

# Logging Configuration
logging.level.com.bank.schoolmanagement=DEBUG
logging.level.org.springframework.web=INFO

# ==========================================
# FLEXCUBE INTEGRATION SETTINGS
# ==========================================
# Source System Identifier (MOBILEBANKING, COUNTER, INTERNETBANKING, USSD)
flexcube.default.source=MOBILEBANKING
# User/System ID for Flexcube authentication
flexcube.default.userid=FLEXSWITCH
# Default branch code
flexcube.default.branch=507
# Product code
flexcube.default.product=PBIL
# Default transaction currency (USD, ZWL, etc.)
flexcube.default.currency=ZWG
# School collection account (destination account for credits)
flexcube.school.collection.account=10101143102432
# Flexcube Token API endpoint (for authentication)
flexcube.token.url=http://10.106.60.5:2011/api/GetToken
# Flexcube Token API Basic Auth credentials
flexcube.token.username=bancabc
flexcube.token.password=password
# Flexcube Payment API endpoint
flexcube.api.url=http://10.106.60.5:2011/api/CreateRTTransactionDrCrNarration
# API timeout (milliseconds)
flexcube.api.timeout=30000
# Enable/disable Flexcube integration (set to false for development/testing)
flexcube.enabled=true
